import React, { Component } from 'react';
import { Link } from 'react-router';

class Signin extends Component {
  render() {
    return (
      <div>
        signin.
        <Link to={'/info'}>aa</Link>
      </div>
    );
  }
}

export default Signin;
